import { FormatMonthPipe } from './format-month.pipe';

describe('FormatMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
